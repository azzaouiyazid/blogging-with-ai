# Add optional dependencies to requirements note

# The project uses readability-lxml and beautifulsoup4 for deeper content extraction.
# If you want the best extraction results, install:
#   pip install readability-lxml beautifulsoup4
# The code will fall back to using meta descriptions and the first paragraphs if readability isn't installed.
